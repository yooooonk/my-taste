const NoDetailBook = ()=>{

    return (
      <div className="BookDetail">
        책을 검색해주세용
      </div>
    );
};

/* AppLayout.propTypes = {
    children : propTypes.node.isRequired
}; */

export default NoDetailBook;