const Dashboard = ()=>{

    return (
      <div className="dashboard-part">
        대시보드
      </div>
    );
};

/* AppLayout.propTypes = {
    children : propTypes.node.isRequired
}; */

export default Dashboard;