import React from 'react';
import PropTypes from 'prop-types';
import { Grid } from '../elements';
//import ImageCarousel from './ImageCarousel';

const Post = (props) => {
  return (
    <Grid>
      {/* <hader>사진 title nickname </hader> */}
      {/* <ImageCarousel /> */}
    </Grid>
  );
};

export default Post;
